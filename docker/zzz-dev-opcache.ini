; Development OPcache overrides — mounted via docker-compose.override.yml
; Overrides docker/php.ini so file changes are picked up without restarting FPM
opcache.validate_timestamps=1
opcache.revalidate_freq=0
